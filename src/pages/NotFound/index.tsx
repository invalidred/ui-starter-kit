import * as React from 'react'
import { RouteComponentProps } from '@reach/router'

const NotFound = (props: RouteComponentProps) => (
  <main>Ooops... Page Not Found</main>
)

export default NotFound
